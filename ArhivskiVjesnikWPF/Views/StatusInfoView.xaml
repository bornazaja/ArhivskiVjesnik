<UserControl x:Class="ArhivskiVjesnikWPF.Views.StatusInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ArhivskiVjesnikWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             FontSize="{StaticResource DefaultFontSize}">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding Message}" Margin="10"/>
            <Button x:Name="OK" Content="OK" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
